# test/fixtures/golden/

Expected outputs for golden-file tests (rendered wikitext, IR JSON dumps, run reports).

Regenerated via `go test -update`. Diff the changes before committing — a golden-file diff is a deliberate behaviour change, not a rubber stamp.
