# test/fixtures/kb/

Snapshot kb directories used by integration + contract tests.

Each subdirectory here is a complete miniature kb (`areas/<id>/...`) that a test can point a `KBSourceAdapter` at. Keep them small and focused on what the test exercises.
