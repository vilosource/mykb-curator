# test/fixtures/specs/

Sample spec files in the format the spec engine consumes (markdown + frontmatter).

Per kind (projection / editorial / hub / runbook), keep at least one valid example and one deliberately-invalid example to exercise spec-validation failures.
